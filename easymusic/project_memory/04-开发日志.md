# 开发日志

## 记录规则

每次完成阶段性工作后，记录：

- 做了什么
- 为什么做
- 涉及哪些模块
- 结果是什么

---

## 2026-04-27

### 工作内容

1. 分析了现有项目结构、表结构、前后台页面和核心业务链路
2. 输出了整体实施规划文档
3. 明确了外部 AI 配置文件路径和使用约束
4. 建立了 `AGENTS.md + project_memory` 的上下文维护机制
5. 补充了 `07-本轮任务.md`，用于维护当前会话级任务上下文
6. 增加了 `08-重开窗口提示词模板.md`，用于重开窗口后快速恢复上下文并继续任务

### 结果

当前项目已完成规划层准备，后续可直接进入第一阶段开发。

---

## 2026-04-27 第二轮

### 工作内容

1. 新增作品发布状态枚举 `DRAFT / PUBLISHED / HIDDEN`
2. 扩展 `music_info` 相关实体、查询对象、Mapper 与 XML，加入 `publish_status`、`publish_time`
3. 新增作品发布状态切换接口 `/my/changePublishStatus`
4. 修复点赞统计闭环，点赞/取消点赞时同步维护 `music_info.good_count`
5. 收紧公共查询口径，只让已发布作品进入首页、最新、推荐、用户主页和点赞列表
6. 增加未发布作品详情访问控制，并限制未发布作品的播放计数与点赞
7. 更新初始化 SQL，并新增增量升级脚本 `stage1_upgrade.sql`
8. 改造前台作品卡片，展示发布状态并支持发布/隐藏/草稿切换
9. 修正后台音乐列表错误筛选项，改为按发布状态筛选
10. 完成后端编译、前台构建、后台构建验证

### 涉及模块

- `easymusic-java/easymusic-common`
- `easymusic-java/easymusic-web`
- `easymusic-java/easymusic-admin`
- `easymusic-front/easymusic-front-web`
- `easymusic-front/easymusic-front-admin`
- `easymusic-java/easymusic_all.sql`
- `easymusic-java/stage1_upgrade.sql`

### 结果

第一阶段“点赞统计闭环 + 作品发布状态最小生命周期”已经形成可运行闭环，项目下一步可以直接进入提示词增强能力开发。

---

## 2026-04-28

### 工作内容

1. 恢复 `AGENTS.md`、`project_memory` 和规划文档上下文
2. 确认 `api的url和key.txt` 实际采用“对话ai / 图片ai”两段式配置
3. 新增 `PromptAssistController`、`PromptAssistService`、`PromptGenerateApi` 后端骨架
4. 新增 `ExternalAiConfigService`，统一解析 `api的url和key.txt`
5. 新增 `prompt_optimize_record` 实体、Mapper、XML、全量 SQL 和 `stage2_prompt_assist_upgrade.sql`
6. 实现提示词增强最小闭环：
   - 优先请求外部对话 AI
   - 请求失败时回退本地结构化结果
   - 调用结果落库到 `prompt_optimize_record`
7. 继续推进到音乐生成链路：
   - 扩展 `music_creation` 的提示词来源字段
   - 扩展 `/my/createMusic` 入参，支持携带原始一句话和提示词增强记录 ID
8. 改造前台创作页，增加一句话输入、智能扩写入口和结构化结果展示
9. 将扩写结果自动回填到音乐生成表单
10. 完成后端编译验证和前台构建验证

### 涉及模块

- `easymusic-java/easymusic-common`
- `easymusic-java/easymusic-web`
- `easymusic-java/easymusic_all.sql`
- `easymusic-java/stage2_prompt_assist_upgrade.sql`

### 结果

第二阶段已经形成“后端骨架 + 创作页接入”的最小可运行版本，当前系统已具备提示词增强入口、结果回填和来源字段透传能力，下一步应进行真实接口联调并准备进入 AI 封面生成阶段。

---

## 2026-04-28 第三轮

### 工作内容

1. 更新 `AGENTS.md` 的工作规则
2. 增加“每完成一个明确阶段且达到可运行、可验证或可演示状态后，必须提交一次代码”的要求
3. 同步更新实现决策记录

### 涉及模块

- `AGENTS.md`
- `project_memory/04-开发日志.md`
- `project_memory/05-实现决策.md`

### 结果

项目协作规则已明确要求阶段完成后进行代码提交，后续每个阶段应保留独立提交记录。

---

## 2026-04-28 第四轮

### 工作内容

1. 恢复 `AGENTS.md`、`project_memory` 和规划文档上下文，确认当前进入第三阶段起点
2. 新增 `MusicCoverController`、`MusicCoverService`、`ImageCreateApi` 后端骨架
3. 新增 `music_cover_creation` 实体、Mapper、XML、全量 SQL 和 `stage3_cover_generation_upgrade.sql`
4. 扩展 `music_info`，新增 `cover_source`、`cover_generate_count`
5. 手动上传封面时同步标记封面来源为手动上传
6. 封面生成接口首轮实现为：
   - 优先请求外部图片 AI
   - 返回异常或缺失配置时回退为本地生成占位封面
   - 成功后回写作品封面并记录生成次数
   - 支持封面价格字典 `cover_price`
7. 扩展积分记录枚举，预留提示词增强和 AI 封面生成的扣减/退款类型
8. 完成后端编译验证

### 涉及模块

- `easymusic-java/easymusic-common`
- `easymusic-java/easymusic-web`
- `easymusic-java/easymusic_all.sql`
- `easymusic-java/stage3_cover_generation_upgrade.sql`

### 结果

第三阶段已经形成“AI 封面生成后端骨架 + 数据结构 + 配置字典 + 本地回退封面”的最小可编译闭环，下一步可以直接接作品卡片、详情页和创作者中心入口。

---

## 2026-04-28 第五轮

### 工作内容

1. 为 AI 封面生成补充最近记录查询接口 `/musicCover/records`
2. 新增前台通用组件 `AICoverDialog`，集中承载封面生成入口、来源展示、生成次数和最近记录列表
3. 改造前台作品卡片 `MusicItem.vue`：
   - 接入 `AI 封面` 操作按钮
   - 展示封面来源和 AI 生成次数
   - 手动上传封面后同步刷新本地状态
4. 改造作品详情页 `MusicDetail.vue`：
   - 接入 `AI 生成封面` 入口
   - 展示封面来源、AI 生成次数和作者侧发布信息
   - 封面更新后同步刷新当前页面和播放器封面状态
5. 完成后端编译验证和前台构建验证

### 涉及模块

- `easymusic-java/easymusic-common`
- `easymusic-java/easymusic-web`
- `easymusic-front/easymusic-front-web`
- `project_memory/01-当前状态.md`
- `project_memory/04-开发日志.md`
- `project_memory/05-实现决策.md`
- `project_memory/06-待办清单.md`
- `project_memory/07-本轮任务.md`

### 结果

第三阶段已经从“后端骨架可编译”推进到“前后端最小闭环可演示”，当前用户已经可以在作品卡片和详情页发起 AI 封面生成，并查看最近生成记录；下一步应转入封面积分链路验证和页面进一步重构。

---

## 2026-04-28 第六轮

### 工作内容

1. 扩展 `MusicInfo` 与 `MusicInfoMapper`，把 `music_creation` 的来源字段并入作品查询结果
2. 调整 `/my/loadMyMusic`，支持按 `publishStatus / musicStatus` 进行作者侧筛选
3. 改造 `My.vue` 为创作者中心布局，增加状态筛选、已加载摘要和刷新入口
4. 改造 `MusicItem.vue`，展示提示词来源、原始需求摘要，并补“继续创作”入口
5. 改造 `MusicDetail.vue`，增加作者侧创作摘要区，展示一句话需求、最终提示词、创作模式和模型
6. 完成后端编译验证和前台构建验证

### 涉及模块

- `easymusic-java/easymusic-common`
- `easymusic-java/easymusic-web`
- `easymusic-front/easymusic-front-web`
- `project_memory/01-当前状态.md`
- `project_memory/04-开发日志.md`
- `project_memory/05-实现决策.md`
- `project_memory/06-待办清单.md`
- `project_memory/07-本轮任务.md`

### 结果

作品查询已经具备提示词来源与创作摘要字段，前台“我的作品”和详情页可以直接展示创作上下文；下一步可以继续做 AI 封面积分链路验证，以及创作页和移动端的进一步收尾。

---

## 2026-04-28 第七轮

### 工作内容

1. 恢复 `AGENTS.md`、`project_memory` 与实施规划上下文，确认本轮优先任务为 AI 封面积分链路验证
2. 为 `easymusic-common` 增加测试依赖，并补充 Surefire 在 Windows 环境下的兼容配置
3. 新增 `MusicCoverServiceImplTest` 单元测试：
   - 验证成功路径会插入生成记录、扣减积分、回写作品封面与生成次数
   - 验证失败路径会执行退款并把记录更新为失败状态
4. 执行定向测试与后端编译验证

### 涉及模块

- `easymusic-java/easymusic-common`
- `project_memory/01-当前状态.md`
- `project_memory/04-开发日志.md`
- `project_memory/05-实现决策.md`
- `project_memory/06-待办清单.md`
- `project_memory/07-本轮任务.md`

### 结果

AI 封面的扣分、失败退款、记录更新和作品封面回写主链路已具备可重复执行的后端自动化验证；在继续遵守“暂不进入真实联调”的前提下，下一步应转入创作页、创作者中心和移动端结构细化。

---

## 2026-04-28 第八轮

### 工作内容

1. 继续恢复 `AGENTS.md`、`project_memory` 与实施规划上下文，确认本轮优先推进前台结构优化和移动端细化
2. 改造 `Idea.vue` 和 `Create.vue`：
   - 将创作页整理为“创作配置 + 最近创作”双栏工作台
   - 新增创作概览卡片、模式说明、结构化建议和模型摘要
   - 去掉歌词区依赖 `100vh` 的固定高度写法
3. 继续改造 `My.vue`、`MusicItem.vue` 与 `MusicDetail.vue`：
   - 创作者中心增加概览卡片和更稳定的列表容器
   - 作品卡片补充播放/点赞摘要，并收敛移动端操作区布局
   - 作品详情补充播放、获赞、创建时间统计卡和“继续创作”入口
4. 修复 `DataLoadMoreList` 的滚动判断：
   - 移除多余的 `window` 滚动监听
   - 补足“内容未撑满容器时自动继续加载”的逻辑
5. 完成前台构建验证

### 涉及模块

- `easymusic-front/easymusic-front-web`
- `project_memory/01-当前状态.md`
- `project_memory/03-问题排查记录.md`
- `project_memory/04-开发日志.md`
- `project_memory/05-实现决策.md`
- `project_memory/06-待办清单.md`
- `project_memory/07-本轮任务.md`

### 结果

第三阶段页面结构优化已经从“作者中心和详情页一轮改造”推进到“创作页工作台化 + 列表滚动容器收敛 + 移动端细化”的可验证版本；下一步应转入后台封面任务查询入口和统计摘要补齐。

---

## 2026-04-28 第九轮

### 工作内容

1. 继续恢复 `AGENTS.md`、`project_memory` 与实施规划上下文，确认本轮优先任务为后台封面任务查询入口与统计摘要
2. 新增 `MusicCoverCreationQuery` 与 `MusicCoverSummaryVO`，为后台 AI 封面任务管理补齐分页查询和聚合摘要结构
3. 扩展 `MusicCoverCreationMapper / MusicCoverService`：
   - 支持后台分页查询封面任务记录
   - 支持按筛选条件聚合总任务数、成功数、失败数、生成中、今日新增和覆盖作品数
4. 新增后台控制器 `MusicCoverAdminController`，补充：
   - `/musicCover/loadCoverRecordList`
   - `/musicCover/loadCoverSummary`
5. 后台前端新增 `CoverRecordList.vue` 页面，并补菜单、路由和接口配置：
   - 支持按用户昵称、作品标题、任务状态、发布状态筛选
   - 展示封面预览、作品信息、任务状态、发布信息、提示词、失败原因和时间
   - 展示总任务数、成功率、生成中、今日新增、覆盖作品摘要卡
6. 完成后端编译验证和后台构建验证

### 涉及模块

- `easymusic-java/easymusic-common`
- `easymusic-java/easymusic-admin`
- `easymusic-front/easymusic-front-admin`
- `project_memory/01-当前状态.md`
- `project_memory/04-开发日志.md`
- `project_memory/05-实现决策.md`
- `project_memory/06-待办清单.md`
- `project_memory/07-本轮任务.md`

### 结果

后台已经具备独立的 AI 封面任务管理入口和统计摘要，第三阶段的“封面生成闭环 + 作者侧入口 + 后台管理入口”进一步完善；下一步应转入后台统计看板和全站移动端观感收尾。

---

## 2026-04-28 第十轮

### 工作内容

1. 继续恢复 `AGENTS.md`、`project_memory` 与实施规划上下文，确认本轮优先任务为后台统计看板雏形
2. 后端新增 `AdminDashboardController / Service / Mapper` 和对应 VO：
   - 实时聚合用户、创作、作品、封面、积分和充值数据
   - 统计总量、今日量、成功率、来源分布和热门排行
3. 后台前端新增 `Dashboard.vue` 独立看板页，并补菜单、路由和登录后默认入口：
   - 展示今日创作、音乐/封面成功率、今日积分消耗、今日充值金额
   - 展示作品状态分布、提示词来源分布、封面来源分布
   - 展示热门模型、热门作品、热门创作者
4. 完成后端编译验证和后台构建验证

### 涉及模块

- `easymusic-java/easymusic-common`
- `easymusic-java/easymusic-admin`
- `easymusic-front/easymusic-front-admin`
- `project_memory/01-当前状态.md`
- `project_memory/04-开发日志.md`
- `project_memory/05-实现决策.md`
- `project_memory/06-待办清单.md`
- `project_memory/07-本轮任务.md`

### 结果

后台统计看板已经形成“实时聚合接口 + 独立页面 + 登录默认入口”的可验证雏形，当前阶段可以把重心切换到首页、最新页、用户主页和移动端观感统一收尾。

---

## 2026-04-28 第十一轮

### 工作内容

1. 继续恢复 `AGENTS.md`、`project_memory` 与实施规划上下文，确认本轮优先任务为首页、最新页、用户主页与移动端布局收口
2. 收敛前台布局层：
   - `Layout.vue` 改为 `100dvh` 高度口径，并统一底部播放器留白
   - `TopPanel.vue`、`LeftSide.vue` 调整移动端顶部导航和侧边栏高度表现
3. 重构首页 `Index.vue`：
   - 改为 Hero + 推荐精选 + 最新发布的统一结构
   - 推荐区保留横向滑动，但补充更清晰的文案层级和操作入口
4. 重构 `Latest.vue` 与 `UserHome.vue`：
   - 最新页改为摘要头图 + 局部滚动列表容器
   - 用户主页改为创作者概要、数据摘要卡和公开作品列表布局
5. 收敛推荐卡片与最新列表项交互：
   - `CommendList.vue` 增加滑动边界保护和窗口缩放后的状态修正
   - `MusicCommendItem.vue`、`MusicLatestItem.vue` 统一卡片样式，并修复移动端无 hover 时操作按钮不可用的问题
6. 完成前台构建验证

### 涉及模块

- `easymusic-front/easymusic-front-web`
- `project_memory/01-当前状态.md`
- `project_memory/03-问题排查记录.md`
- `project_memory/04-开发日志.md`
- `project_memory/05-实现决策.md`
- `project_memory/06-待办清单.md`
- `project_memory/07-本轮任务.md`

### 结果

首页、最新页、用户主页和布局层已经完成一轮统一收口，当前前台核心公共页面与创作页、创作者中心的视觉口径更接近，移动端高度与操作可用性问题进一步减少；下一步应继续补剩余页面的视觉统一，并视答辩展示需要补统计看板趋势维度。

---

## 2026-04-28 第十二轮

### 工作内容

1. 继续恢复 `AGENTS.md`、`project_memory` 与实施规划上下文，确认本轮优先任务仍为前台全站视觉统一与移动端最后收尾
2. 收敛作品详情页作者侧管理路径：
   - 在 `MusicDetail.vue` 补齐“修改标题 / 上传封面 / AI 重做封面 / 发布 / 隐藏 / 设为草稿”入口
   - 作者不再必须回到作品列表才能完成封面与发布状态管理
   - 详情页补充作者状态提示，并把主内容区统一为更完整的卡片式摘要布局
3. 补强创作页提交前摘要：
   - `Create.vue` 新增“本次提交摘要”区
   - 集中展示创作类型、模式、提示词来源、模型、曲风、情绪、人声和当前提示词
   - 移动端下摘要卡自动切到双列/单列，减少滚动确认成本
4. 完成前台构建验证

### 涉及模块

- `easymusic-front/easymusic-front-web`
- `project_memory/01-当前状态.md`
- `project_memory/04-开发日志.md`
- `project_memory/05-实现决策.md`
- `project_memory/06-待办清单.md`
- `project_memory/07-本轮任务.md`

### 结果

作品详情页已经补齐作者侧发布管理入口，创作页也补上提交前摘要区，作者主路径从“创作 -> 查看详情 -> 管理封面/发布”进一步闭合；下一步可继续补残余页面细节，或转入统计看板趋势维度的答辩展示增强。

---

## 2026-04-28 第十三轮

### 工作内容

1. 继续恢复 `AGENTS.md`、`project_memory` 与实施规划上下文，确认本轮目标为完成前台残余页面收口
2. 重构充值中心与套餐卡片：
   - `Buy.vue` 改为积分中心 Hero + 套餐列表布局
   - `ProductItem.vue` 改为统一卡片式套餐信息和操作区
3. 收口支付、登录和资料管理弹窗：
   - `Pay.vue` 统一订单确认、扫码支付和成功态样式，并把支付帮助在小屏下改为点击触发
   - `LoginAndRegister.vue`、`EditUser.vue`、`UpdatePassword.vue`、`IntegralRecord.vue` 统一为当前前台表单与记录视觉口径
4. 适配通用封面裁剪弹窗：
   - `ImageCoverCut.vue` 从固定双栏改为小屏可堆叠布局
   - 上传头像、上传封面等路径同步受益
5. 完成前台构建验证

### 涉及模块

- `easymusic-front/easymusic-front-web`
- `project_memory/01-当前状态.md`
- `project_memory/03-问题排查记录.md`
- `project_memory/04-开发日志.md`
- `project_memory/05-实现决策.md`
- `project_memory/06-待办清单.md`
- `project_memory/07-本轮任务.md`

### 结果

前台残余页面已经完成一轮统一收口，购买、登录、资料管理、积分记录和封面裁剪路径不再明显割裂；前台页面收尾已基本完成，下一步应切换到统计看板趋势维度、真实接口联调和答辩材料整理。

---

## 2026-04-28 第十四轮

### 工作内容

1. 继续恢复 `AGENTS.md`、`project_memory` 与实施规划上下文，确认本轮优先任务为补统计看板趋势维度与答辩展示细节
2. 扩展后台统计看板后端：
   - 新增 `AdminDashboardTrendVO`
   - 扩展 `AdminDashboardVO` 和 `AdminDashboardMapper`
   - 为 `/dashboard/loadDashboard` 补充最近 7 日用户、创作、成功、发布、封面、积分消耗和充值趋势数据
3. 重构后台 `Dashboard.vue`：
   - 新增“近 7 日主链路趋势”区块，展示创作、成功、发布和封面任务节奏
   - 新增“运营趋势与展示口径”区块，展示积分消耗、充值趋势和答辩说明
   - 为今日摘要卡补充较昨日变化和近 7 日累计描述
4. 完成后端编译验证和后台构建验证

### 涉及模块

- `easymusic-java/easymusic-common`
- `easymusic-java/easymusic-admin`
- `easymusic-front/easymusic-front-admin`
- `project_memory/01-当前状态.md`
- `project_memory/04-开发日志.md`
- `project_memory/05-实现决策.md`
- `project_memory/06-待办清单.md`
- `project_memory/07-本轮任务.md`

### 结果

后台统计看板已经从“实时聚合雏形”推进到“总览 + 近 7 日趋势 + 统计口径说明”的可验证版本，更适合答辩展示；下一步应切换到 AI 封面真实接口联调与记录口径终验。

---

## 2026-04-28 第十五轮

### 工作内容

1. 在真实启动环境下排查前台“智能扩写地址不存在”和“创作音乐失败”问题
2. 确认前台后端最初启动的是旧版 `jar`，重新按当前源码重打并启动 `easymusic-web`
3. 确认音乐创作失败的直接原因是天普乐音乐接口返回 `Authorization is blank.`
4. 改造 `MusicCreteApi4TianpuyueImpl`：
   - 增加 `TIANPUYUE_API_KEY` 和 `courseOrderId` 启动前校验
   - 解析第三方接口 `status / message`
   - 把“音乐创作失败”改为更明确的配置或接口错误提示
5. 完成后端编译验证，并重新启动前台后端服务

### 涉及模块

- `easymusic-java/easymusic-common`
- `project_memory/03-问题排查记录.md`
- `project_memory/04-开发日志.md`

### 结果

已明确音乐创作失败并非前端表单问题，而是本地环境未配置 `TIANPUYUE_API_KEY`；后端已补更明确的报错链路，后续只需补齐音乐接口凭证即可继续真实联调。

---

## 2026-04-28 第十九轮

### 工作内容

1. 恢复 `AGENTS.md`、`project_memory` 与实施规划上下文，确认当前仍处于 AI 封面真实接口联调前的最后功能收口阶段
2. 复核 AI 封面提示词链路，明确“立即生成”使用的默认提示词来源：
   - 优先读取智能扩写结构化结果中的 `imagePrompt / visualStyle`
   - 未命中时回退为作品标题、原始需求和歌词摘要组合
   - 最终仍由 `ImageCreateApiOpenAiImpl#normalizePrompt(...)` 封装为图片模型请求 prompt
3. 补齐 AI 封面提示词可编辑能力：
   - 后端新增 `/musicCover/promptPreview`
   - `/musicCover/generate` 支持接收手动编辑后的 `prompt / style`
   - 前台 `AICoverDialog` 增加默认词加载、来源说明、恢复默认和手动编辑后生成
4. 完成后端编译、前台构建，并重新打包启动前台后端 `8090`

### 涉及模块

- `easymusic-java/easymusic-common`
- `easymusic-java/easymusic-web`
- `easymusic-front/easymusic-front-web`
- `project_memory/01-当前状态.md`
- `project_memory/03-问题排查记录.md`
- `project_memory/04-开发日志.md`
- `project_memory/05-实现决策.md`
- `project_memory/06-待办清单.md`
- `project_memory/07-本轮任务.md`

### 结果

AI 封面弹窗现在不再是“黑盒生成”。用户可以先看到系统默认提示词，再自行修改“封面提示词”和“视觉风格”后点击生成；前后端代码已通过构建验证，`8090` 也已切到包含该能力的新版本，下一步应直接进入真实图片接口效果与记录口径终验。

---

## 2026-04-28 第二十轮

### 工作内容

1. 根据用户反馈排查“智能扩写回填的音乐提示词和封面提示词太简单，不够专业”的问题
2. 确认症结不只在远程 AI，而在于本地 fallback 和最终后处理逻辑过弱，且扩写接口没有利用当前创作上下文
3. 为扩写接口新增上下文输入：
   - `musicType / modeType`
   - `musicGenre / musicEmotion / musicSex`
4. 重构 `PromptGenerateApiOpenAiImpl`：
   - 强化对话 AI system prompt
   - 对远程请求超时时间单独放宽到 35 秒
   - 无论远程是否成功，都统一把结果收口为更专业的音乐制作 brief
   - 封面提示词改为直接围绕同一音乐主题、情绪、场景与视觉风格生成，形成闭环
5. 改造创作页 `Create.vue`：
   - 扩写请求携带当前表单上下文
   - 回填时同步更新曲风、情绪、人声，避免提示词和表单分离
6. 完成后端编译、前台构建，并重新打包启动前台后端 `8090`

### 涉及模块

- `easymusic-java/easymusic-common`
- `easymusic-java/easymusic-web`
- `easymusic-front/easymusic-front-web`
- `project_memory/01-当前状态.md`
- `project_memory/02-需求变更记录.md`
- `project_memory/03-问题排查记录.md`
- `project_memory/04-开发日志.md`
- `project_memory/05-实现决策.md`
- `project_memory/06-待办清单.md`
- `project_memory/07-本轮任务.md`

### 结果

当前“智能扩写”已经从简单模板升级为更偏专业制作 brief 的结果，且封面提示词会围绕同一音乐主题、情绪和场景生成；后端与前端均已通过验证，`8090` 已切到新版本，下一步应直接用真实请求验证实际生成质量。

---

## 2026-04-28 第二十一轮

### 工作内容

1. 继续围绕“智能扩写回填结果仍然不够专业”做后端收口，复核 `PromptGenerateApiOpenAiImpl`
2. 进一步强化对话 AI system prompt，明确要求音乐提示词和封面提示词共享同一组叙事锚点与情绪弧线
3. 重构本地 professional fallback：
   - `musicPrompt` 改为统一输出“核心母题 / 风格定位 / 情绪弧线 / 速度律动 / 和声色彩 / 配器层次 / 结构设计 / 人声设计 / 混音目标 / 封面联动关键词”
   - `imagePrompt` 改为统一输出“同一作品母题 / 场景 / 主体 / 镜头语言 / 色彩光影 / 材质质感 / 构图”的闭环封面描述
   - 标题与标签改为围绕同一叙事线索生成，不再使用过于泛化的旧模板
4. 保持“远程结果优先，本地只补洞”的策略，避免成功返回的高质量远程结果再次被本地模板重写
5. 新增并扩展 `PromptGenerateApiOpenAiImplTest`，覆盖：
   - 远程专业结果保留
   - 远程结果过薄时回退到本地专业模板
   - 悬疑纯音乐场景下音乐与封面的闭环 fallback
6. 执行验证：
   - `mvn -q -pl easymusic-common -Dtest=PromptGenerateApiOpenAiImplTest test`
   - `mvn -q -pl easymusic-web -am -DskipTests compile`

### 涉及模块

- `easymusic-java/easymusic-common`
- `easymusic-java/easymusic-web`
- `project_memory/01-当前状态.md`
- `project_memory/03-问题排查记录.md`
- `project_memory/04-开发日志.md`
- `project_memory/05-实现决策.md`
- `project_memory/06-待办清单.md`
- `project_memory/07-本轮任务.md`

### 结果

“智能扩写”这一层现在不只是避免了远程结果被覆盖，还把本地兜底结果本身也提升到了更接近真实音乐制作 brief 的水平。即使远程接口暂时失败，页面回填出来的 `musicPrompt / imagePrompt / titleSuggestions / tags` 也会共享同一叙事母题和封面线索，不再退化成模板味很重的泛化结果。

---

## 2026-04-28 第二十二轮

### 工作内容

1. 排查创作页点击“智能扩写”后直接提示“请求超时，请稍后重试”的问题
2. 复核前端 `Request.js` 与创作页请求链路，确认全局 axios 默认超时为 10 秒
3. 结合 `.runlogs/backend-web.log` 核对真实联调耗时，确认 `promptAssist/generate` 实际常在 15 到 20 多秒内成功返回
4. 修改 `Create.vue` 中 `generatePromptAssist` 的请求配置，仅为智能扩写接口单独放宽超时时间到 60 秒
5. 执行前端构建验证：
   - 受限环境首次构建因 `esbuild` 子进程 `spawn EPERM` 失败
   - 切换到沙箱外执行 `npm.cmd run build`，验证通过

### 涉及模块

- `easymusic-front/easymusic-front-web/src/views/idea/Create.vue`
- `project_memory/03-问题排查记录.md`
- `project_memory/04-开发日志.md`
- `project_memory/06-待办清单.md`
- `project_memory/07-本轮任务.md`

### 结果

智能扩写这条前端请求不再受 10 秒全局超时限制，真实远程调用有足够时间拿到后端返回结果。后端日志已经证明该链路可在超出 10 秒的情况下成功完成，因此这次修复能直接消除“明明后端成功了，但前端先超时”的假失败问题。

---

## 2026-04-28 第二十三轮

### 工作内容

1. 根据用户反馈排查“点击创作音乐第一次提示请求超时，但刷新后作品已是生成中”的问题
2. 复核前端 `Request.js` 与创作页 `Create.vue`，确认：
   - 全局 axios 默认超时仍为 10 秒
   - `createMusic` 请求尚未像“智能扩写”一样单独配置更长超时
3. 结合当前后端创建链路，确认这是典型的前端假超时：
   - 后端先落创作记录和 `CREATING` 状态
   - 前端再因为 10 秒超时提前中断
4. 修改 `Create.vue` 中 `createMusic` 请求，仅对该接口单独放宽超时时间到 120 秒
5. 执行前端构建验证：
   - 沙箱内首次构建因 `esbuild` 子进程 `spawn EPERM` 失败
   - 切换到沙箱外执行 `npm.cmd run build`，验证通过

### 涉及模块

- `easymusic-front/easymusic-front-web/src/views/idea/Create.vue`
- `easymusic-front/easymusic-front-web/src/utils/Request.js`
- `project_memory/03-问题排查记录.md`
- `project_memory/04-开发日志.md`
- `project_memory/06-待办清单.md`
- `project_memory/07-本轮任务.md`

### 结果

创作音乐这条慢接口现在不再受全局 10 秒超时限制，首次点击时前端不会再在后端已进入“生成中”后错误提示失败。当前这条前端假超时问题已收口，下一步应直接进入真实音乐生成链路验证，再继续封面和远程对话 AI 的联调终验。

---

## 2026-04-28 第二十四轮

### 工作内容

1. 根据用户反馈排查“点击创作音乐提示请求参数错误”的问题
2. 复核 `.runlogs/backend-web.log`，确认真实报错不是整包参数缺失，而是：
   - `ConstraintViolationException: createMusic.musicSex`
   - 原因是提交的人声标签长度超过后端旧限制 `@Size(max = 5)`
3. 修改前端 `Create.vue`：
   - 新增 `normalizeMusicSex(...)`
   - 智能扩写回填、再次扩写请求、创作音乐提交、编辑已有创作回显统一走短标签归一化
4. 修改后端提示词增强：
   - `PromptGenerateApiOpenAiImpl` 新增人声标签标准化
   - 远程 AI 返回“清冷女声主唱”等长文本时，统一收口为 `女声 / 男声 / 合唱 / 童声 / 纯音乐`
5. 修改后端控制器兼容旧请求：
   - `MyController#createMusic(...)` 的 `musicSex` 校验上限放宽到 30
   - `PromptAssistController#generate(...)` 的 `musicSex` 校验上限放宽到 30
6. 补充后端定向单测，覆盖“远程返回长人声标签时自动收口”
7. 执行验证：
   - `mvn -q -pl easymusic-common -Dtest=PromptGenerateApiOpenAiImplTest test`
   - `mvn -q -pl easymusic-web -am -DskipTests compile`
   - 前台 `npm.cmd run build`
8. 因后端运行中的旧 jar 占用目标文件，先停止旧 `8090` 进程，再重新 `package` 并重启前台后端

### 涉及模块

- `easymusic-front/easymusic-front-web/src/views/idea/Create.vue`
- `easymusic-java/easymusic-common/src/main/java/com/easymusic/api/PromptGenerateApiOpenAiImpl.java`
- `easymusic-java/easymusic-common/src/test/java/com/easymusic/api/PromptGenerateApiOpenAiImplTest.java`
- `easymusic-java/easymusic-web/src/main/java/com/easymusic/controller/MyController.java`
- `easymusic-java/easymusic-web/src/main/java/com/easymusic/controller/PromptAssistController.java`
- `project_memory/01-当前状态.md`
- `project_memory/03-问题排查记录.md`
- `project_memory/04-开发日志.md`
- `project_memory/06-待办清单.md`
- `project_memory/07-本轮任务.md`

### 结果

“创作音乐请求参数错误”已确认是 `musicSex` 超长引发的参数校验失败，不是模型、积分或音乐创建主链路本身故障。当前前后端都已对人声标签做收口和兼容处理，`8090` 也已切到新版本，下一步应直接在页面实测一次“智能扩写后立即创作音乐”的真实链路。

---

## 2026-04-28 第二十五轮

### 工作内容

1. 根据用户反馈排查“智能扩写、AI 封面没有扣积分，积分记录也没有新增”的问题
2. 直接核对代码与实库，确认这是两类不同根因：
   - `PromptAssistServiceImpl` 从未接入积分扣减逻辑，`prompt_optimize_record.integral` 固定为 `0`
   - `MusicCoverServiceImpl` 已有扣减逻辑，但价格仅读 Redis 字典缓存，缓存缺 `cover_price` 分组时直接退为 `0`
3. 使用本地 MySQL 实测验证：
   - `sys_dict` 中已有 `cover_price/default=10`
   - 新增补入 `prompt_price/default=5`
   - `user_integral_record` 中 `record_type in (5,6,7,8)` 之前为 `0`
   - `music_cover_creation.integral` 与 `prompt_optimize_record.integral` 历史最近记录均为 `0`
4. 改造 `PromptAssistServiceImpl`：
   - 新增 `prompt_price` 字典读取
   - 成功时扣减 `PROMPT_OPTIMIZE`
   - 失败时退款 `PROMPT_OPTIMIZE_BACK`
   - `prompt_optimize_record.integral` 改为真实值
5. 改造 `MusicCoverServiceImpl`：
   - 价格读取从“仅 Redis”改为“Redis 缺组时回源 DB 并回写缓存”
6. 新增 / 扩展单测：
   - `PromptAssistServiceImplTest` 覆盖提示词增强扣分与失败退款
   - `MusicCoverServiceImplTest` 覆盖 AI 封面 Redis 缺价时回源 DB 仍正常扣分
7. 补齐 SQL：
   - `stage2_prompt_assist_upgrade.sql` 增加 `prompt_price/default=5`
   - `easymusic_all.sql` 增加 `prompt_price` 默认数据
   - 顺手修正 `stage3_cover_generation_upgrade.sql` 中 `dict_name` 字段为实际库结构 `dict_desc`
8. 完成验证并切运行环境：
   - `mvn -q -pl easymusic-common "-Dtest=PromptGenerateApiOpenAiImplTest,MusicCoverServiceImplTest,PromptAssistServiceImplTest" test`
   - `mvn -q -pl easymusic-web -am -DskipTests compile`
   - 重新 `package` 并重启前台后端 `8090`

### 涉及模块

- `easymusic-java/easymusic-common`
- `easymusic-java/easymusic-web`
- `easymusic-java/stage2_prompt_assist_upgrade.sql`
- `easymusic-java/stage3_cover_generation_upgrade.sql`
- `easymusic-java/easymusic_all.sql`
- `project_memory/01-当前状态.md`
- `project_memory/03-问题排查记录.md`
- `project_memory/04-开发日志.md`
- `project_memory/05-实现决策.md`
- `project_memory/06-待办清单.md`
- `project_memory/07-本轮任务.md`

### 结果

当前两条 AI 扣费链路已经收口：智能扩写不再是“只落库不结算”，AI 封面也不再因为 Redis 老缓存缺少 `cover_price` 而默认免费。新的请求会按 `prompt_price/default=5` 和 `cover_price/default=10` 产生积分扣减与积分记录；历史旧记录未追补扣费，保持现状不回溯修改。

---

## 2026-04-28

### 工作内容

1. 根据用户反馈复核“积分记录”弹窗现在到底记录什么，以及为什么用户会觉得消费积分没有记录
2. 核对前后端链路后确认：后端积分接口已经查询全量流水，但前台弹窗展示语义过弱，更像“充值记录”
3. 继续排查前台积分刷新逻辑，确认智能扩写与 AI 封面成功后没有主动刷新全局积分余额
4. 重构 `IntegralRecord.vue` 为账单视图，补齐获取 / 消费 / 退款 / 业务来源 / 业务号展示
5. 在 `Create.vue` 与 `AICoverDialog.vue` 中补 `userInfoStore.updateLastReloadTime()`，并完成前台构建验证

### 涉及模块

- `easymusic-front/easymusic-front-web/src/views/my/IntegralRecord.vue`
- `easymusic-front/easymusic-front-web/src/views/idea/Create.vue`
- `easymusic-front/easymusic-front-web/src/component/biz/AICoverDialog.vue`
- `project_memory/02-需求变更记录.md`
- `project_memory/03-问题排查记录.md`
- `project_memory/05-实现决策.md`
- `project_memory/06-待办清单.md`
- `project_memory/07-本轮任务.md`

### 结果

积分记录弹窗现在已经明确承载“全部积分流水”，而不再只是看起来像充值记录：

1. 顶部增加“当前积分 / 已加载账单 / 已加载消费 / 已加载入账”摘要
2. 每条记录明确标出“获取 / 消费 / 退款”以及对应业务说明和业务号
3. 智能扩写、AI 封面成功扣分后会主动刷新全局积分余额，减少“实际上已扣分但界面还没变”的误判
4. 前台 `npm.cmd run build` 已通过

---

## 2026-04-28

### 工作内容

1. 根据新的用户反馈继续优化“积分记录”弹窗的阅读负担和视觉层级
2. 复核上一版实现后确认：虽然功能语义已经补齐，但说明文字偏多、卡片表达偏重，不够像账单列表
3. 重构 `IntegralRecord.vue` 的信息结构：
   - 顶部说明压缩为一句话
   - 摘要卡片文案改短
   - 单条记录改为单列账单样式
   - 主信息只保留业务类型、积分变化、时间、业务号和必要金额
4. 完成前台构建验证

### 涉及模块

- `easymusic-front/easymusic-front-web/src/views/my/IntegralRecord.vue`
- `project_memory/02-需求变更记录.md`
- `project_memory/03-问题排查记录.md`
- `project_memory/05-实现决策.md`
- `project_memory/06-待办清单.md`
- `project_memory/07-本轮任务.md`

### 结果

积分记录弹窗已经从“解释型面板”进一步收口为更偏真实产品账单的样式：

1. 解释文案更短，用户不用先读一大段说明
2. 记录列表改为单列，更适合按时间扫读
3. 单条记录的信息噪音更少，重点直接落在“什么业务、扣了多少、什么时候发生”
4. 前台 `npm.cmd run build` 已通过

补充微调：

1. 顶部原本的一整行说明文字仍然偏灰、偏弱，不够好读
2. 已继续收口为 `充值 / 消费 / 退款 / 时间倒序` 四个标签
3. 这样用户不需要先读句子，打开弹窗就能快速扫到关键信息
4. 前台 `npm.cmd run build` 再次通过

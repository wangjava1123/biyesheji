# 本轮任务

## 使用方式

这个文件只记录“当前正在推进的单个任务”。

和其他文件的区别：

- `01-当前状态.md` 记录项目整体状态
- `06-待办清单.md` 记录中短期待办列表
- `07-本轮任务.md` 只记录这一轮正在做的最具体任务

每次开始新任务时，应更新本文件；每次结束当前会话前，如果任务未完全结束，也要更新本文件，保证下次能直接接上。

---

## 当前任务

### 任务名称

搭建提示词增强后端骨架并接入外部 AI 配置读取层

### 任务目标

为主线中的“用户一句话描述需求 -> AI 扩写成结构化提示词”建立最小可扩展实现基础，包括：

1. 后端 Controller / Service / Api 分层骨架
2. `prompt_optimize_record` 数据表设计
3. 外部 AI 配置读取封装
4. 后续前端创作页接入所需的统一返回结构

### 当前进展

已完成前置工作：

1. 项目级上下文管理与日志追踪机制已经建立
2. 点赞统计闭环已经修复
3. 作品发布状态最小生命周期已经落地
4. 数据库全量 SQL 和第一阶段增量脚本已经补齐
5. 前后台最小展示与操作闭环已经完成
6. 后端编译、前台构建、后台构建均已通过

当前尚未开始编码：

1. `PromptAssistController`
2. `PromptAssistService`
3. `PromptGenerateApi`
4. `prompt_optimize_record` 表
5. `api的url和key.txt` 解析层

### 当前阻塞

暂无明确阻塞。

### 下一步

1. 先梳理现有外部 API 适配层写法，复用现有风格搭骨架
2. 设计 `prompt_optimize_record` 表和对应实体/Mapper
3. 封装读取 `api的url和key.txt` 的配置类，不在业务代码中硬编码 URL 和 Key

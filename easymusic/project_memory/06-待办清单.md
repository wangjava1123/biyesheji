# 待办清单

## 已完成

1. 设计并落地第一阶段数据库变更方案
2. 修复点赞统计闭环问题
3. 增加作品发布状态和作品生命周期字段
4. 增加作品发布状态的前后台最小可用展示与操作
5. 完成第一阶段首轮编译与构建验证
6. 完成提示词增强后端接口骨架
7. 设计并落地 `prompt_optimize_record` 表及增量脚本
8. 封装 `api的url和key.txt` 的统一读取层
9. 扩展 `music_creation` 和 `/my/createMusic`，支持提示词增强来源字段
10. 完成第二阶段后端首轮编译验证
11. 完成创作页提示词增强入口的首轮接入
12. 完成前台构建验证
13. 完成 AI 封面生成后端第一轮骨架
14. 完成第三阶段后端编译验证
15. 完成 AI 封面生成记录查询接口 `/musicCover/records`
16. 完成作品卡片、作品详情页和通用弹窗的 AI 封面前端接入
17. 完成第三阶段前端构建验证
18. 完成阶段提交：`feat: 补齐AI封面前端接入闭环`
19. 补齐 `music_creation` 到作品查询的展示字段
20. 完成创作者中心和作品详情页的一轮结构优化
21. 完成后端编译与前台构建验证
22. 完成 AI 封面积分扣减、失败退款和记录更新的后端单元测试验证
23. 完成创作页工作台化、结构化建议展示和模型摘要补强
24. 完成创作者中心概览卡片、作品卡片统计摘要和详情页统计卡补强
25. 完成 `DataLoadMoreList` 滚动容器收敛与前台构建验证
26. 完成后台封面任务查询入口与统计摘要
27. 完成后端编译与后台构建验证
28. 完成后台统计看板雏形
29. 完成登录后默认进入统计看板与后台菜单接入
30. 完成后端编译与后台构建验证
31. 完成前台布局层 `100dvh` 与移动端顶栏收口
32. 完成首页重构与推荐/最新区块统一
33. 完成最新页、用户主页重构及局部滚动区修正
34. 修复推荐卡片与最新列表项在移动端 hover 不可用的问题
35. 完成前台构建验证
36. 完成作品详情页作者侧管理入口补齐
37. 完成创作页“本次提交摘要”区与移动端摘要收口
38. 完成前台构建验证
39. 完成充值中心、支付弹窗、登录注册和资料管理相关残余页面收口
40. 完成 `ImageCoverCut.vue` 小屏堆叠式适配
41. 完成前台构建验证
42. 完成后台统计看板最近 7 日趋势维度与答辩展示细节增强
43. 完成后端编译与后台构建验证
44. 完成 AI 封面默认提示词预览与手动编辑生成能力
45. 完成后端编译、前台构建和前台后端 `8090` 重启验证
46. 完成智能扩写专业级提示词增强与音乐-封面闭环收口
47. 完成后端编译、前台构建和前台后端 `8090` 重启验证
48. 完成智能扩写“远程结果优先保留、本地只补洞”的后处理修复
49. 完成智能扩写本地 fallback 的第二轮专业化增强，统一收口为共享叙事母题的音乐/封面/标题/标签结果
50. 完成 `PromptGenerateApiOpenAiImplTest` 扩展，并通过定向测试与 `easymusic-web` 编译验证
51. 完成创作页智能扩写请求超时排查，并将该接口前端超时时间单独放宽到 60 秒
52. 完成前端 `npm.cmd run build` 验证
53. 完成创作页“创作音乐”请求超时排查，并将该接口前端超时时间单独放宽到 120 秒
54. 完成前端 `npm.cmd run build` 验证
55. 完成创作页“请求参数错误”排查，并定位到 `musicSex` 超长导致的后端参数校验失败
56. 完成前后端 `musicSex` 归一化与控制器兼容修复，并通过后端定向单测、后端编译、前台构建验证
57. 完成前台后端 `8090` 重新打包与重启验证
58. 完成“智能扩写不扣积分、AI封面不扣积分”问题排查，并定位为“前者未实现、后者缓存缺价退0”
59. 完成提示词增强积分结算、AI封面价格回源、SQL默认字典与相关单测补齐
60. 完成前台后端 `8090` 重新打包与重启验证
61. 完成“积分记录”弹窗账单化重构，并补齐智能扩写、AI 封面成功后的前台余额刷新
62. 完成“积分记录”弹窗第二轮可读性收口，改为更接近账单列表的单列样式

## 当前优先级

### P1

1. 实测创作音乐真实链路，确认首次提交不再出现前端假超时，且作品状态流转与创作者中心展示一致
2. 实测“智能扩写后立即创作音乐”的真实链路，确认不再出现 `musicSex` 参数错误
3. 实测智能扩写与 AI 封面新的积分扣减和积分记录是否与页面展示一致
4. 恢复并验证 `promptAssist/generate` 的远程对话 AI 调用，确认 `responseSource=remote` 时不再被本地模板覆盖
5. 进入 AI 封面真实接口联调与记录口径终验
6. 整理统计看板与前台页面的验证记录、截图和阶段提交

### P2

1. 为真实接口联调补测试记录和验证说明
2. 视联调结果微调提示词增强、统计看板或后台任务页口径
3. 补最终答辩素材

### P3

1. 最终统一真实联调与演示验证
2. 按答辩场景补最终截图和流程图

## 当前阻塞

- `promptAssist/generate` 真实联调时，远程对话 AI 当前返回“接口请求失败”，导致暂时只能走 fallback，尚未完成“远程成功分支不被覆盖”的终验

## 协作约束

- 新增规则：每完成一个明确阶段且达到可运行、可验证或可演示状态后，必须提交一次代码
- 真实联调阶段继续避免在日志、截图和记忆文件中暴露完整外部 Key

## 下一步最小可执行任务

1. 先直接做一次“智能扩写 -> 创作音乐 -> AI封面”的真实提交流程，确认积分余额、积分记录、`prompt_optimize_record.integral` 与 `music_cover_creation.integral` 四处口径一致
2. 再排查 `.runlogs/backend-web.log` 中对话 AI 请求失败的原因，核对 `api的url和key.txt`、远程地址和启动环境
3. 远程恢复后，用“深夜城市女声流行歌”和“暴雨夜追踪悬疑纯音乐”两组样例验证智能扩写质量与音乐-封面闭环，再继续 AI 封面真实接口联调与记录口径终验
